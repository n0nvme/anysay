[tool.poetry]
name = "pixelart_gen"
version = "0.1.0"
description = ""
authors = ["n0nvme <svmpl3nvm3@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.6"
pillow = "^7.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
